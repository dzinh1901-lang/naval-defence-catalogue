# ── Secret template ────────────────────────────────────────────────────────────
# This file is a TEMPLATE. Do NOT commit real secret values.
# In CI/CD, generate this file via:
#
#   kubectl create secret generic naval-dt-secrets \
#     --namespace=naval-dt \
#     --from-literal=DATABASE_URL='postgresql://naval:<password>@postgres:5432/naval_dt' \
#     --from-literal=JWT_SECRET='<min-32-char-random-secret>' \
#     --from-literal=POSTGRES_PASSWORD='<db-password>' \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# Or use Sealed Secrets / External Secrets Operator for GitOps-friendly secret management.
apiVersion: v1
kind: Secret
metadata:
  name: naval-dt-secrets
  namespace: naval-dt
  labels:
    app.kubernetes.io/name: naval-dt
    app.kubernetes.io/managed-by: kustomize
type: Opaque
stringData:
  DATABASE_URL: "postgresql://naval:REPLACE_ME@postgres:5432/naval_dt"
  JWT_SECRET: "REPLACE_WITH_MIN_32_CHAR_RANDOM_SECRET"
  POSTGRES_PASSWORD: "REPLACE_ME"
