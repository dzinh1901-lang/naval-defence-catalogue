jobs:
  typecheck:
    needs: install
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/naval_defence_catalogue
    steps:
      # existing steps

  build:
    needs: install
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/naval_defence_catalogue
    steps:
      # existing steps

  prisma:
    needs: install
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/naval_defence_catalogue
    steps:
      # existing steps
